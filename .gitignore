# venv / build
.venv/
**/.venv/
__pycache__/
*.pyc
cdk.out/
node_modules/

# AgentCore / infra local state
**/.bedrock_agentcore/
**/.bedrock_agentcore.yaml
infra/config.json

# 실행 산출물 (스크린샷/영상/생성 스크립트)
web/out/
artifacts/
demovideo/
*.mp4
**/_live.png
err.log
.DS_Store

# APK 는 예외적으로 포함 (모바일 데모 대상 앱). 소스(app/)는 미포함.
!assets/deliveryapp-debug.apk

# 서브디렉토리 README 는 github 에 올리지 않음 (내부 설계 노트/고객 사례 포함). 루트만 공개.
**/README.md
!/README.md
# 단, deploy 배포 가이드는 공개 (민감내용 없음).
!deploy/README.md

# 다이어그램 편집 원본은 github 에 올리지 않음 (렌더된 .jpg 만 공개).
diagrams/*.drawio
diagrams/*.html

